<div dir="rtl">

# PasarGuard Admin Report 📊

اسکریپت گزارش‌گیری حرفه‌ای برای **PasarGuard / Marzban Panel** که به‌صورت خودکار گزارش‌های **روزانه / هفتگی / ماهانه** را از دیتابیس استخراج می‌کند و برای شما در **تلگرام** ارسال می‌کند.

✅ هدف اصلی: **جلوگیری از ضرر مالی**  
چون هرگونه **ساخت کاربر، افزایش حجم، ریست مصرف، و نامحدود کردن حجم (Unlimited)** به‌صورت شفاف در گزارش‌ها ثبت و اعلام می‌شود.

> ⚠️ نکته مهم: این پروژه روی «Unlimited حجمی» حساس است چون می‌تواند باعث ضرر شود.  
> Unlimited زمانی (expire) در این پروژه مهم نیست.

---

## ✨ امکانات اصلی

### ✅ گزارش روزانه (Daily)
- برای **هر ادمین به‌صورت جداگانه** ارسال می‌شود.
- فقط اکشن‌هایی که هزینه‌ی حجمی دارند محاسبه می‌شوند.
- اگر کاربری **Unlimited حجمی** شود، در گزارش مشخص می‌شود و لیست Unlimited‌ها هم نمایش داده می‌شود.

مواردی که در گزارش روزانه پوشش داده می‌شود:
- ساخت کاربر جدید (`INSERT`) → محاسبه **کل حجم پلن**
- افزایش/تغییر حجم (`CHANGE_LIMIT`) → محاسبه **فقط اختلاف حجم جدید و قدیم** (اگر مثبت باشد)
- ریست مصرف (`RESET_USAGE`) → محاسبه **کل حجم پلن** (چون کاربر دوباره حجم کامل گرفته)
- نامحدود کردن حجم (`UNLIMITED`) → نمایش به‌صورت `unlimited`
- افزایش زمان (`CHANGE_EXPIRE`) → فقط نمایش `extend` (هزینه حجمی ندارد)

---

### ✅ خلاصه روزانه (Daily Summary)
- یک پیام جداگانه برای **جمع کل همه ادمین‌ها** ارسال می‌شود.
- برای هر ادمین نشان می‌دهد: **حجم کل + تعداد Unlimited**
- در انتها نیز **جمع کل حجم** و **جمع Unlimited‌ها** مشخص می‌شود.

---

### ✅ گزارش هفتگی (Weekly)
- بازه: **شنبه تا جمعه**
- یک پیام برای همه ادمین‌ها
- شامل:
  - حجم کل هر ادمین در هفته
  - تعداد Unlimited هر ادمین
  - لیست نام کاربران Unlimited هر ادمین

---

### ✅ گزارش ماهانه (Monthly)
- بازه: **ماه شمسی قبلی**
- در **روز اول هر ماه شمسی** اجرا می‌شود
- شامل:
  - حجم کل هر ادمین در ماه
  - تعداد Unlimited و لیست آن‌ها

---

## ✅ قوانین محاسبه دقیق (ضد ضرر مالی)

این اسکریپت فقط اکشن‌های «حجمی/پولی» را حساب می‌کند تا گزارش دقیق باشد:

| Action در دیتابیس | معنی | محاسبه / خروجی |
|---|---|---|
| `INSERT` | ساخت کاربر جدید | کل `data_limit_new` |
| `CHANGE_LIMIT` | افزایش/تغییر حجم | فقط `(new - old)` اگر مثبت باشد |
| `RESET_USAGE` | ریست مصرف | کل `data_limit_new` |
| `UNLIMITED` | نامحدود شدن حجم | فقط نمایش `unlimited` |
| `CHANGE_EXPIRE` | افزایش زمان | فقط نمایش `extend` |
| `UPDATE` | آپدیت‌های سیستمی | نادیده گرفته می‌شود |
| `DELETE` | حذف کاربر | نادیده گرفته می‌شود |

✅ **Unlimited حجمی** یعنی وقتی `data_limit_new` برابر **`NULL`** یا **`0`** باشد.  
⚠️ Unlimited زمانی (expire) مهم نیست — فقط Unlimited حجمی مهم است.

---

## 📌 نمونه خروجی (Example Output)

### ✅ گزارش روزانه
</div>

```text
Daily report - 1404-10-06
Admin: admin58586

- ali: +50.00 GB
- reza2022: unlimited
- m12: +20.00 GB | reset

Total: 70.00 GB

Unlimited users:
reza2022
```
