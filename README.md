# PasarGuard Admin Report 📊

این پروژه یک اسکریپت گزارش‌گیری حرفه‌ای برای **PasarGuard / Marzban Panel** است که به‌صورت خودکار گزارش‌های **روزانه / هفتگی / ماهانه** را از دیتابیس دریافت می‌کند و برای شما در تلگرام ارسال می‌کند.

✅ هدف اصلی: **جلوگیری از ضرر مالی**  
✅ چون هرگونه افزایش حجم، ریست مصرف، ساخت اکانت جدید و نامحدود کردن حجم، در گزارش‌ها مشخص می‌شود.

---

## ✨ امکانات اصلی

### ✅ گزارش روزانه (Daily)
برای هر ادمین به‌صورت جداگانه ارسال می‌شود:

- ایجاد کاربر جدید (`INSERT`) → محاسبه کامل حجم ساخته شده
- افزایش حجم کاربر (`CHANGE_LIMIT`) → محاسبه فقط اختلاف حجم جدید و قبلی
- ریست مصرف (`RESET_USAGE`) → محاسبه حجم کامل پلن (چون کاربر دوباره حجم کامل گرفته)
- نامحدود کردن حجم (`Unlimited`) → نمایش به‌صورت `unlimited`

---

### ✅ گزارش هفتگی (Weekly)
- بازه: **شنبه تا جمعه**
- برای همه ادمین‌ها در یک پیام ارسال می‌شود
- علاوه بر حجم، تعداد کاربران `unlimited` و لیست آن‌ها نیز نمایش داده می‌شود

---

### ✅ گزارش ماهانه (Monthly)
- بازه: **ماه شمسی قبلی**
- فقط در روز اول هر ماه شمسی ارسال می‌شود
- حجم کلی هر ادمین + `unlimited`ها نمایش داده می‌شود

---

## ✅ قوانین محاسبه دقیق (ضد ضرر مالی)

این اسکریپت تنها اکشن‌های پولی را محاسبه می‌کند:

| Action در دیتابیس | معنی | محاسبه |
|------------------|------|--------|
| INSERT | ساخت کاربر جدید | کل `data_limit_new` |
| CHANGE_LIMIT | افزایش/تغییر حجم | فقط `new - old` اگر مثبت باشد |
| RESET_USAGE | ریست مصرف | کل `data_limit_new` |
| CHANGE_EXPIRE | افزایش زمان | فقط نمایش `extend` (هزینه حجمی ندارد) |
| UPDATE | آپدیت‌های سیستمی | نادیده گرفته می‌شود |
| DELETE | حذف کاربر | نادیده گرفته می‌شود |

✅ **Unlimited حجمی** زمانی است که `data_limit_new == 0` یا `NULL` باشد.  
⚠️ Unlimited زمانی (expire) اهمیتی ندارد.

---

## 📌 نمونه خروجی (Example Output)

### ✅ گزارش روزانه
```text
Daily report - 1404-10-05
Admin: admin58586

- ali: +50.00 GB
- reza2022: unlimited
- m12: +20.00 GB | reset

Total: 70.00 GB

Unlimited users:
reza2022
