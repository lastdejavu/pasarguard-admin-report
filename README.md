sudo tee /opt/pasarguard-admin-report/README.md >/dev/null <<'MD'
# PasarGuard Admin Report 📊 (Daily Digest)

گزارش روزانه‌ی فعالیت ادمین‌های **PasarGuard** به تلگرام شما ارسال می‌شود تا جلوی ضرر/سوءاستفاده گرفته شود.

## خروجی نمونه
1404-10-08
Admin: admin

ali: +20.00 GB

hamed: +30.00 GB

sami: unlimited (was 100.00 GB)

Total: 50.00 GB  


✅ فقط چیزهای مهم و کم‌نویز:
- ساخت یوزر محدود → `+X GB`
- افزایش حجم → `+X GB`
- ساخت یوزر انلیمیت → `unlimited`
- تبدیل محدود → انلیمیت → `unlimited (was XX GB)` (برای جلوگیری از ضرر)

> نکته: کاهش حجم را گزارش نمی‌کند (طبق سیاست شما).

---

## پیش‌نیازها
- Ubuntu (ترجیحاً 22.04)
- PasarGuard نصب‌شده با MySQL داخل Docker
- یک Telegram Bot Token و Chat ID

---

## نصب (فقط 1 دستور)
```bash
git clone https://github.com/lastdejavu/pasarguard-admin-report.git
cd pasarguard-admin-report
sudo bash install.sh


در حین نصب از شما این‌ها را می‌پرسد:

Bot Token

Chat ID

نام کانتینر MySQL (پیش‌فرض: pasarguard-mysql-1)

پسورد root MySQL (اگر PasarGuard روی مسیر /opt/pasarguard/.env باشد معمولاً خودش تشخیص می‌دهد)

زمان‌بندی گزارش

هر روز ساعت 00:00 به وقت ایران (Asia/Tehran) گزارش روز قبل ارسال می‌شود.

کران داخل root نصب می‌شود و لاگ‌ها در این فایل ذخیره می‌شوند:

/var/log/pasarguard-admin-report.log

تست سریع

اگر می‌خواهید همین الان یک تست بزنید:

cd /opt/pasarguard-admin-report
source .venv/bin/activate
python3 daily_digest.py


(اگر دیروز رویدادی ثبت نشده باشد، ممکن است پیام نفرستد.)
