sudo tee /opt/pasarguard-admin-report/README.md >/dev/null <<'MD'
# PasarGuard Admin Report 📊 (Daily Digest)

گزارش روزانه‌ی فعالیت ادمین‌های **PasarGuard** به تلگرام شما ارسال می‌شود تا جلوی ضرر/سوءاستفاده گرفته شود.

## خروجی نمونه
1404-10-08
Admin: admin

ali: +20.00 GB

hamed: +30.00 GB

sami: unlimited (was 100.00 GB)

Total: 50.00 GB  


✅ فقط چیزهای مهم و کم‌نویز:
- ساخت یوزر محدود → `+X GB`
- افزایش حجم → `+X GB`
- ساخت یوزر انلیمیت → `unlimited`
- تبدیل محدود → انلیمیت → `unlimited (was XX GB)` (برای جلوگیری از ضرر)

> نکته: کاهش حجم را گزارش نمی‌کند .

---

## پیش‌نیازها
- Ubuntu (ترجیحاً 22.04)
- PasarGuard نصب‌شده با MySQL داخل Docker
- یک Telegram Bot Token و Chat ID

---

**یعنی چی؟**
- `+10.00 GB` یعنی ادمین امروز/دیروز حجم یوزر را زیاد کرده یا یوزر محدود ساخته.
- `unlimited` یعنی یوزر unlimited ساخته شده.
- `unlimited (was 100.00 GB)` یعنی یوزری که قبلاً محدود بوده (مثلاً 100GB) تبدیل شده به unlimited — این مورد برای جلوگیری از ضرر بسیار مهم است.

---


---

## چه چیزهایی را گزارش می‌کند؟

این اسکریپت با **Trigger** روی جدول `users` در دیتابیس PasarGuard، رویدادها را در جدول `admin_report_events` ثبت می‌کند و سپس روزی یک‌بار گزارش را برای هر ادمین جدا ارسال می‌کند:

✅ ساخت یوزر محدود (`USER_CREATED`)  
✅ ساخت یوزر نامحدود (`UNLIMITED_CREATED`)  
✅ افزایش حجم (`DATA_LIMIT_CHANGED` فقط افزایش‌ها)  
✅ تبدیل محدود → نامحدود (`LIMIT_TO_UNLIMITED`)  
✅ ریست مصرف (`USAGE_RESET`)  

### سیاست مهم مالی (برای جلوگیری از ضرر)
اگر یوزر محدود باشد و ادمین **ریست** کند، این پروژه به صورت پیش‌فرض **کل سقف فعلی یوزر** را شارژ حساب می‌کند.  
مثال: یوزر سقف 100GB دارد، 50GB مصرف کرده؛ با ریست → **+100GB** در گزارش حساب می‌شود.

این رفتار با ENV قابل تغییر است:
- `CHARGE_FULL_LIMIT_ON_RESET=1` (پیش‌فرض: 1)

---

## نیازمندی‌ها

- PasarGuard روی سرور شما نصب شده باشد و MySQL آن **داخل Docker** اجرا شود.
- فایل env پاسارگارد موجود باشد:
  - پیش‌فرض: `/opt/pasarguard/.env`
  - باید شامل این مقادیر باشد:
    - `MYSQL_ROOT_PASSWORD=...`
    - `DB_NAME=...` (اگر نبود، پیش‌فرض `pasarguard`)
- نیازی به نصب MySQL روی خود سرور نیست (اسکریپت از `docker exec` استفاده می‌کند).

---

## نصب با یک خط (پیشنهادی)

### حالت تعاملی (از شما توکن و چت آیدی می‌پرسد)
```bash
curl -fsSL https://raw.githubusercontent.com/lastdejavu/pasarguard-admin-report/main/install.sh | sudo bash
