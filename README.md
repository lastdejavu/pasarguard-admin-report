# PasarGuard Admin Report 📊

اسکریپت گزارش‌گیری حرفه‌ای برای **PasarGuard / Marzban Panel** که به‌صورت خودکار گزارش‌های **روزانه / هفتگی / ماهانه** را از دیتابیس استخراج می‌کند و برای شما در **تلگرام** ارسال می‌کند.

✅ هدف اصلی: **جلوگیری از ضرر مالی**  
✅ چون هرگونه **ساخت کاربر جدید، افزایش حجم، ریست مصرف، و نامحدود شدن حجم (Unlimited حجمی)** در گزارش‌ها ثبت و قابل پیگیری می‌شود.

---

## ✨ امکانات اصلی

### ✅ گزارش روزانه (Daily)
برای هر ادمین **به‌صورت جداگانه** ارسال می‌شود و موارد زیر را نشان می‌دهد:
- ساخت کاربر جدید (`INSERT`) → محاسبه **کل حجم ساخته شده**
- افزایش/تغییر حجم (`CHANGE_LIMIT`) → محاسبه **فقط اختلاف مثبت** (new - old)
- ریست مصرف (`RESET_USAGE`) → محاسبه **کل حجم پلن** (چون کاربر دوباره حجم کامل می‌گیرد)
- نامحدود شدن حجم (`UNLIMITED`) → نمایش به‌صورت `unlimited`
- افزایش زمان (`CHANGE_EXPIRE`) → فقط نمایش `extend` (اثر مالیِ حجمی ندارد)

---

### ✅ گزارش هفتگی (Weekly)
- بازه: **شنبه تا جمعه (شمسی)**
- همه ادمین‌ها **در یک پیام**
- علاوه بر حجم، **تعداد unlimited** و **لیست کاربران unlimited** نیز نمایش داده می‌شود

---

### ✅ گزارش ماهانه (Monthly)
- بازه: **ماه شمسی قبلی**
- در **روز اول هر ماه شمسی**
- حجم کلی هر ادمین + تعداد و لیست `unlimited` ها نمایش داده می‌شود

---

## ✅ قوانین محاسبه (ضد ضرر مالی)

این اسکریپت فقط اکشن‌های «حجمی» را مالی حساب می‌کند:

| Action | معنی | محاسبه |
|------:|------|--------|
| `INSERT` | ساخت کاربر جدید | کل `data_limit_new` |
| `CHANGE_LIMIT` | افزایش/تغییر حجم | فقط `new - old` (اگر مثبت باشد) |
| `RESET_USAGE` | ریست مصرف | کل `data_limit_new` |
| `UNLIMITED` | نامحدود شدن حجم | فقط نمایش `unlimited` |
| `CHANGE_EXPIRE` | افزایش زمان | فقط نمایش `extend` |
| `UPDATE` | آپدیت سیستمی | نادیده گرفته می‌شود |
| `DELETE` | حذف کاربر | نادیده گرفته می‌شود |

✅ **Unlimited حجمی** یعنی `data_limit_new` برابر `0` یا `NULL` باشد.  
⚠️ Unlimited زمانی (expire) مهم نیست — فقط **حجم نامحدود** مهم است.

---

## 📌 نمونه خروجی

### ✅ گزارش روزانه
```text
Daily report - 1404-10-06
Admin: admin58586

- ali: +50.00 GB
- reza2022: unlimited
- m12: +20.00 GB | reset

Total: 70.00 GB

Unlimited users:
reza2022


##
git clone https://github.com/lastdejavu/pasarguard-admin-report.git
cd pasarguard-admin-report
sudo bash install.sh

